<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>List of user</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-blue-100 text-blue-500">
  <div class="max-w-4xl mx-auto mt-10 p-6 bg-white shadow-lg rounded-lg">
    <div class="flex justify-between items-center mb-6">
      <h1 class="text-2xl font-bold">User Info</h1>
      <a href="/Add"
      class="bg-blue-600 text-white px-4 py-2 rounded-lg overflow-hidden">Add New</a>
    </div>
  <table border="1"
  class="min-w-full bg-white border border-blue-200 rounded-lg overflow-hidden">
  <tr>
    <th>User Id</th>
    <th>User Name</th>
    <th>User Place</th>
    <th colspan="2">Operations</th>
  </tr>
  <% user.forEach(user => { %>
   <tr class="hover:bg-blue-100">
   
        <td class="py-2 px-4 border-b"><%= user.id %></td>
        <td class="py-2 px-4 border-b"><%= user.name %></td>
        <td class="py-2 px-4 border-b"><%= user.place %></td>
        <td class="py-2 px-4 border-b">
          <a href="edit/<%= user.id %>" >Edit</a>
        </td>
        <td><a href="delete/<%= user.id %>" onclick="return confirm(`Are You Sure you want to delete <%= user.name %>`)">Delete</a></td>
   </tr> 
   <% }) %>
    <!-- <% user.forEach(user => { %>
        <p><strong>User Id:</strong><%= user.id %></p>
        <p><strong>User Name:</strong><%= user.name %></p>
        <p><strong>User Place:</strong><%= user.place %></p>
      <% }) %> -->
    </table>
  </div>
</body>
</html>